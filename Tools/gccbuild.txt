Prepare the environment:
  1. Open a command prompt (cmd.exe)
  2. Run:
       cd <OSSBuild>\Tools\
       "init msys.bat" gccbuild

Start the build:
  1. Open an msys prompt:
  2. Run:
       cd <OSSBuild>/Tools/
       ./gccbuild-w64.sh